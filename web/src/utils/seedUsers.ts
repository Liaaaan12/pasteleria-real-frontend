// Dev-only seeding disabled: external database is now the source of truth.
// Keeping a no-op function to preserve imports without breaking builds.
export function seedUsersFromJson(): void {
    // intentionally left blank
}

export default seedUsersFromJson;
